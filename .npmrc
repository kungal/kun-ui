# Keep the dependency tree strict so packages can only import what they
# explicitly declare — important for a multi-package library where a
# framework-agnostic package (@kun/core) must never accidentally pull in
# Vue/React from a sibling.
shamefully-hoist=false
