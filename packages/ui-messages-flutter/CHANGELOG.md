## 2.37.0

- Version bump only, to stay in lockstep with the KunUI release train.

## 2.36.0

- First release. KunUI's own strings as `KunMessages`, generated from the same
  catalogs `@kungal/ui-core` bundles: `zh-CN` (the built-in default) and `en`.
