import { KUN_CATALOG_EN } from '@kungal/ui-core/locale/en'
import { enUS } from 'date-fns/locale'
import type { KunLocale } from './types'

const en: KunLocale = { ...KUN_CATALOG_EN, dateLocale: enUS }

export default en
